import ContactForm from './ContactForm';

const ContactPage = () => {
  return (
    <main className="w-full min-h-screen bg-[color:var(--color-light1)] dark:bg-[color:var(--color-dark1)]">
      <ContactForm />
    </main>
  );
};

export default ContactPage;